<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sunstar.mapper.CartMapper">

	<insert id="addCart" parameterType="com.sunstar.dto.CartDTO">
	<selectKey resultType="int" keyProperty="cart_no" order="BEFORE">
	select Max(cart_no)+1 from cart
	</selectKey>
	insert into cart(cart_no, id, product_code, cart_quantity) values('', #{id}, #{product_code}, #{cart_quantity});
	</insert>
	
</mapper>